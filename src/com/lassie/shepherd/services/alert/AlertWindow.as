package com.lassie.shepherd.services.alert{	import com.lassie.shepherd.services.ServiceWindow;	import flash.display.Sprite;	import flash.text.TextField;	import flash.text.TextFieldAutoSize;	import flash.events.MouseEvent;	import flash.events.Event;		public final class AlertWindow extends ServiceWindow	{		public var messageText:TextField;				private var _buttons:Sprite;		private var _closeActions:Array = new Array(Event.CLOSE);			// --------------------------------------------------	//  Constructor	// --------------------------------------------------				public function AlertWindow():void		{			super();						messageText.autoSize = TextFieldAutoSize.LEFT;			_buttons = new Sprite();		}				protected override function uninit():void		{			reset();		}			// --------------------------------------------------	//  Getters / setters	// --------------------------------------------------				public function get message():String		{			return messageText.text;		}				public function set message(mssg:String):void		{			messageText.text = mssg;			_buttons.y = messageText.y + messageText.height + 15;			resizeWindow(width, height);		}				public function setButtons(csv:String):void		{			var list:Array = csv.split(",");						for each (var j:String in list)			{							}			addChild(_buttons);		}				public function addCloseAction(action:String):void		{					}				public function reset():void		{			while(_buttons.numChildren > 0)			{				_buttons.removeChildAt(0);			}						if (this.contains(_buttons))			{				removeChild(_buttons);			}		}			// --------------------------------------------------	//  Public methods	// --------------------------------------------------			private function _onCloseClick(evt:Event):void		{			//dispatchEvent(new Event(Event.CLOSE, true));		}			}}