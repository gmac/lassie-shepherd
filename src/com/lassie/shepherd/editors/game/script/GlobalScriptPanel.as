package com.lassie.shepherd.editors.game.script{	import com.lassie.shepherd.editors.game.ui.PanelDisplays;	import flash.events.Event;		public final class GlobalScriptPanel extends ScriptPanel	{		public function GlobalScriptPanel():void		{			super(ScriptRecordSet.GLOBAL, PanelDisplays.GLOBAL_SCRIPT);			checkEnter.visible = false;			checkExit.visible = false;		}				/* 		* depricated controls for setting a "startGame" script.		*		protected override function populateRecord():void		{			super.populateRecord();			var $default:Boolean = (lsGameData.startGame == selectedScript.id);			checkEnter.selected = $default;			checkEnter.enabled = !$default;		}		protected override function onSelectEnter(evt:Event):void		{			lsGameData.startGame = checkEnter.selected ? selectedScript.id : "";			checkEnter.enabled = false;			_controller.populate();		}		*/	}}